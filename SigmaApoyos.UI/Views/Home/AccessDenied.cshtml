@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="surface-card empty-state mx-auto" style="max-width: 620px;">
    <div class="empty-icon">!</div>
    <span class="page-eyebrow">Permisos insuficientes</span>
    <h1 class="page-title h2 mt-2">No puedes acceder a esta opción</h1>
    <p class="mb-4">Tu cuenta no tiene el permiso necesario. Si consideras que es un error, consulta con la persona administradora.</p>
    <a class="btn btn-primary" asp-controller="Home" asp-action="Index">Volver al inicio</a>
</div>
